{
  "name": "@chronelle/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm messages && pnpm --filter @chronelle/api-client build && next build",
    "dev": "pnpm messages && pnpm --filter @chronelle/api-client build && next dev",
    "messages": "node scripts/build-messages.mjs",
    "start": "node scripts/start-standalone.mjs",
    "sandbox": "pnpm messages && pnpm --filter @chronelle/api-client build && node sandbox/build.mjs",
    "test": "pnpm messages && pnpm --filter @chronelle/api-client build && vitest run",
    "test:unit": "pnpm messages && vitest run",
    "typecheck": "pnpm messages && pnpm --filter @chronelle/api-client build && tsc -p tsconfig.json --noEmit",
    "typecheck:unit": "pnpm messages && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@chronelle/api-client": "workspace:*",
    "@chronelle/schemas": "workspace:*",
    "@tanstack/react-query": "5.102.8",
    "@tanstack/react-table": "8.21.3",
    "next": "16.3.5",
    "next-intl": "4.14.5",
    "react": "19.3.0",
    "react-dom": "19.3.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "6.9.1",
    "@testing-library/react": "16.3.3",
    "@testing-library/user-event": "14.6.7",
    "esbuild": "0.28.2",
    "jsdom": "26.1.0"
  }
}
