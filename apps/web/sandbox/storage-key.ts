/**
 * Where the sandbox keeps its records in localStorage. A module of its own
 * so a browser test can clear the key without loading the store.
 */
export const sandboxStorageKey = "chronelle.design-sandbox.v1";
