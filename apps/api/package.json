{
  "name": "@chronelle/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "files": [
    "dist/**/*.js",
    "dist/**/*.d.ts"
  ],
  "scripts": {
    "benchmark:cloudbase": "node --env-file-if-exists=../../.env dist/benchmark-cloudbase.js",
    "build": "pnpm --filter @chronelle/schemas build && pnpm --filter @chronelle/object-model build && tsc -p tsconfig.build.json",
    "dev": "pnpm --filter @chronelle/schemas build && pnpm --filter @chronelle/object-model build && tsx watch src/server.ts",
    "start": "node dist/server.js",
    "seed:test-data": "node --env-file-if-exists=.env dist/seed-test-data.js",
    "test": "pnpm --filter @chronelle/schemas build && pnpm --filter @chronelle/object-model build && vitest run",
    "test:unit": "vitest run",
    "typecheck": "pnpm --filter @chronelle/schemas build && pnpm --filter @chronelle/object-model build && tsc -p tsconfig.json --noEmit",
    "typecheck:unit": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/nodemailer": "^8.0.1",
    "cos-nodejs-sdk-v5": "3.0.0"
  },
  "dependencies": {
    "@chronelle/authorization": "workspace:*",
    "@chronelle/db": "workspace:*",
    "@chronelle/object-model": "workspace:*",
    "@chronelle/schemas": "workspace:*",
    "@chronelle/storage": "workspace:*",
    "drizzle-orm": "0.45.2",
    "fastify": "5.12.4",
    "nodemailer": "^10.0.9",
    "zod": "4.6.5"
  }
}
