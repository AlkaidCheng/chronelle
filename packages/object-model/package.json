{
  "name": "@chronelle/object-model",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "pnpm --filter @chronelle/authorization build && pnpm --filter @chronelle/storage build && tsc -p tsconfig.build.json",
    "test": "pnpm --filter @chronelle/authorization build && pnpm --filter @chronelle/storage build && vitest run",
    "typecheck": "pnpm --filter @chronelle/authorization build && pnpm --filter @chronelle/storage build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@chronelle/authorization": "workspace:*",
    "@chronelle/db": "workspace:*",
    "@chronelle/storage": "workspace:*",
    "drizzle-orm": "0.45.2"
  }
}
