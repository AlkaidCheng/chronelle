{
  "name": "@chronelle/object-model",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "files": [
    "dist/**/*.js",
    "dist/**/*.d.ts"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "baseline-revisions": "node dist/baseline-cli.js",
    "build": "pnpm --filter @chronelle/schemas build && pnpm --filter @chronelle/authorization build && pnpm --filter @chronelle/storage build && tsc -p tsconfig.build.json",
    "test": "pnpm --filter @chronelle/schemas build && pnpm --filter @chronelle/authorization build && pnpm --filter @chronelle/storage build && vitest run",
    "typecheck": "pnpm --filter @chronelle/schemas build && pnpm --filter @chronelle/authorization build && pnpm --filter @chronelle/storage build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@chronelle/schemas": "workspace:*",
    "@chronelle/authorization": "workspace:*",
    "@chronelle/db": "workspace:*",
    "@chronelle/storage": "workspace:*",
    "drizzle-orm": "0.45.2"
  }
}
