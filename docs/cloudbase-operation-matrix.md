# CloudBase operation matrix

This matrix is the migration inventory for the current API. It records the
consistency and security contract that must remain true when an operation is
served by a different backend. It is intentionally conservative: an operation
marked PostgreSQL-only must not be moved to the CloudBase RDB gateway merely
because its SQL can be expressed as a read or write request.

## Contract vocabulary

- **Read** means no canonical state changes and no audit event is required.
- **CAS** means an update must include the current object `version` and reject
  a stale version.
- **Audit** means the mutation and its audit event are committed as one logical
  operation.
- **Cross-object** means the operation changes more than one canonical object,
  relation, grant, revision, or recovery record.
- **CloudBase candidate** means a repository boundary exists or can be added
  without changing the API response contract. It is not approval to switch the
  operation in production.

## Read operations

| API surface                                                                     | Service boundary                                                                    | Contract                                                                                                                                                                                                                                           | Current route                                                                                                                                                           | CloudBase status                                                                                                                                                                                                                                                                             |
| ------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Event list                                                                      | `EventReadRepository`                                                               | The caller's own workspace's Events as its member beside the ones shared from other workspaces (an active grant on the Event, no membership), permission-filtered, deleted rows excluded, deterministic cursor, per-item access, first-page counts | `GET /api/events`                                                                                                                                                       | Candidate; adapter reads memberships and grants across workspaces through the table route and applies the same two rules                                                                                                                                                                     |
| Calendar projection                                                             | `CalendarReadRepository`                                                            | Canonical event IDs, active `includes` relations, inherited grants, date ordering                                                                                                                                                                  | `GET /api/events/:id/calendar`                                                                                                                                          | Candidate; adapter and opt-in runtime flag exist                                                                                                                                                                                                                                             |
| Event detail                                                                    | `ProjectionReadRepository`                                                          | Root authorization plus child relations, attached documents, and projection consistency                                                                                                                                                            | `GET /api/events/:id/detail`                                                                                                                                            | Candidate; adapter and opt-in runtime flag exist                                                                                                                                                                                                                                             |
| To-do, timeline, itinerary, expense, reminder, people projections               | `ProjectionReadRepository`                                                          | Canonical IDs, active `includes` relations, inherited grants, projection ordering                                                                                                                                                                  | `GET /api/events/:id/{todos,timeline,itinerary,expenses,reminders,people}`                                                                                              | Candidate; adapter and opt-in runtime flag exist                                                                                                                                                                                                                                             |
| Object search                                                                   | `SearchReadRepository`                                                              | Full-text ranking, cursor envelope, workspace and permission predicate                                                                                                                                                                             | `GET /api/search`                                                                                                                                                       | Candidate; adapter calls `chronelle_object_search` (migration 0021)                                                                                                                                                                                                                          |
| Single object, allowed actions, visible objects                                 | `ObjectReadRepository`                                                              | Live object only, roles from membership plus direct and live-scope grants, typed state                                                                                                                                                             | `GET /api/objects/:id`, `/access`, typed `GET` routes                                                                                                                   | Candidate; adapter and opt-in runtime flag exist                                                                                                                                                                                                                                             |
| Relation and removed-relation pages                                             | `RelationReadRepository`                                                            | Object view access, other endpoint visible (removed: editable source), id-desc cursor                                                                                                                                                              | `GET /api/objects/:id/relations`, `/removed-relations`                                                                                                                  | Candidate; adapter and opt-in runtime flag exist                                                                                                                                                                                                                                             |
| Share list                                                                      | `GrantReadRepository`                                                               | Recovery-level access on the resource, expired grants excluded, creation order                                                                                                                                                                     | `GET /api/objects/:id/shares`                                                                                                                                           | Candidate; adapter and opt-in runtime flag exist                                                                                                                                                                                                                                             |
| Revision list and single revision                                               | `RevisionReadRepository`                                                            | Object view access, newest version first, snapshot schema check                                                                                                                                                                                    | `GET /api/objects/:id/revisions`, `/revisions/:version`                                                                                                                 | Candidate; adapter and opt-in runtime flag exist                                                                                                                                                                                                                                             |
| Trash list and recovery preview                                                 | `RecoveryReadRepository`                                                            | Recovery predicate over tombstones, id-desc cursor, blocked-scope reason                                                                                                                                                                           | `GET /api/trash`, `GET /api/objects/:id/recovery-preview`                                                                                                               | Candidate; adapter and opt-in runtime flag exist                                                                                                                                                                                                                                             |
| Command state                                                                   | `CommandReadRepository`                                                             | Caller's stack version, undo head with edit access and version check, redo head only when available                                                                                                                                                | `GET /api/commands`                                                                                                                                                     | Candidate; adapter calls `chronelle_command_state` (migration 0024)                                                                                                                                                                                                                          |
| Revision comparison and restoration preview                                     | `ObjectRestorationService` over `ObjectReadRepository` and `RevisionReadRepository` | View access, revision snapshots decoded once, restorable-content policy in the application                                                                                                                                                         | `GET /api/objects/:id/revisions/compare`, `/revisions/:version/restore-preview`                                                                                         | Candidate; composed from the object and revision adapters                                                                                                                                                                                                                                    |
| Storage inventory                                                               | `StorageInventoryReadRepository`                                                    | Workspace Owner only, referenced keys of one provider, bounded sets, classification in the application                                                                                                                                             | `GET /api/workspace/storage-inventory`                                                                                                                                  | Candidate; adapter calls `chronelle_storage_references` (migration 0025) with the storage provider's listing                                                                                                                                                                                 |
| Event page layout and its history                                               | `EventLayoutReadRepository`                                                         | View access on a live Event, newest version first, history position applied after the read                                                                                                                                                         | `GET /api/events/:id/layout`, `/layout/history`                                                                                                                         | Candidate; adapter reads `event_page_revisions` through the table route                                                                                                                                                                                                                      |
| Identity: sign-in, linking, session, accessible workspaces, account preferences | `IdentityStore` and `WeChatAuthStore` (API package)                                 | Provider identities normalize to one canonical user; WeChat proofs are remotely verified and consumed once; linking requires that user; session and workspace access are evaluated from canonical user ID; preferences are unchanged               | `POST /api/auth/development/sign-in`, `POST /api/auth/wechat`, `POST /api/auth/wechat/link`, `GET /api/auth/session`, `PATCH /api/auth/me`, every authenticated request | Candidate; migration 0071 adds `chronelle_user_session_resolve`, `chronelle_wechat_exchange`, and `chronelle_wechat_identity_link`; sign-in and preferences retain `chronelle_identity_sign_in` and `chronelle_user_preferences_update`; accessible-workspace listing still uses table reads |
| Credentials: password account, attempt lock, verification, reset                | `CredentialStore` (API package)                                                     | Password hash checked in the API; one open code per purpose with counted mismatches; email verified once; reset replaces the hash and clears the lock; both audited in the personal workspace                                                      | `POST /api/auth/sign-up`, `verify-email`, `verify-email/resend`, `sign-in`, `password-reset`, `password-reset/confirm`                                                  | Candidate; adapter calls the `chronelle_password_*`, `chronelle_email_verified`, and `chronelle_verification_*` functions (migrations 0032 and 0033)                                                                                                                                         |
| Friends: requests, invitations, connections                                     | `FriendStore` (API package)                                                         | One live connection per pair; one pending invitation per requester and address; daily cap and resend interval; only display name and email cross accounts; audited in the actor's personal workspace                                               | `GET /api/friends`, `POST /api/friends/invitations`, `requests/:id/accept` or `decline`, `DELETE /api/friends/:id`                                                      | Candidate; adapter calls the `chronelle_friend_*` functions: list, invite, respond, withdraw, remove, resend, and invitations_claim (migration 0051)                                                                                                                                         |
| Shares waiting on an invitation                                                 | `PendingShareStore` (API package)                                                   | Queued by whoever can share the resource on a request or invitation the caller sent; listed and taken back with recovery-level access; granted or lapsed when the request is answered                                                              | `GET /api/objects/:id/shares` (`pending`), `POST /api/shares/pending`, `DELETE /api/shares/pending/:id`                                                                 | Candidate; adapter calls `chronelle_pending_share_list`, `_create`, and `_revoke`; settlement runs inside the redefined `chronelle_friend_respond`, `_withdraw`, and `_invitations_claim` (migration 0052)                                                                                   |
| Workspace members                                                               | `MembershipStore` (API package)                                                     | Any member lists; an Owner adds a friend as viewer or editor and removes a member other than the personal owner and themselves; audited in the workspace                                                                                           | `GET`, `POST`, `DELETE /api/workspaces/current/members`                                                                                                                 | Candidate; adapter calls `chronelle_workspace_member_list`, `_add`, and `_remove` (migration 0052)                                                                                                                                                                                           |
| Sessions: issue, resolve, revoke                                                | `SessionStore` (API package)                                                        | Token digest only; live means unexpired and not revoked; last-seen advanced at most every five minutes; revocation audited in the personal workspace                                                                                               | `POST /api/auth/development/sign-in`, every authenticated request, `DELETE /api/auth/session`, `DELETE /api/auth/sessions`                                              | Candidate; adapter calls `chronelle_session_create`, `chronelle_session_resolve`, `chronelle_session_revoke`, and `chronelle_sessions_revoke_all` (migrations 0030 and 0031)                                                                                                                 |

Read adapters must return canonical IDs and the same externally visible
resource shape. A gateway API key does not authorize a user; the adapter must
still evaluate the Chronelle principal, workspace, grant expiry, inheritance,
and deletion rules.

## Mutations

| Operation family                       | Examples                                                                           | Required guarantees                                                                                                                                                                       | Backend decision                                                                                                                                                                                                                                                                                                                                                                                                             |
| -------------------------------------- | ---------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Event create                           | `POST /api/events`, linked Event creation                                          | Authorization, typed validation, audit event, revision, generated identity                                                                                                                | `EventWriteRepository`; CloudBase adapter calls `chronelle_event_create` (migrations 0012/0013, 0059, 0061) behind `CLOUDBASE_WRITES_ENABLED`                                                                                                                                                                                                                                                                                |
| Event update                           | `PATCH /api/events/:id`                                                            | Authorization, CAS on `version`, merged-state validation, audit, revision                                                                                                                 | `EventWriteRepository`; CloudBase adapter calls `chronelle_event_update` (migrations 0012/0013, 0059, 0061) behind `CLOUDBASE_WRITES_ENABLED`                                                                                                                                                                                                                                                                                |
| Task collection                        | `GET /api/tasks`                                                                   | Authorized `view` per candidate, status, label, assignee, and due range filters, due/name/updated order, cursor bound to caller and query                                                 | `TaskReadRepository`; CloudBase adapter reads `objects` and `tasks` through the table route and re-applies visibility in application code                                                                                                                                                                                                                                                                                    |
| Labels: list, create, rename, delete   | `GET/POST /api/labels`, `PATCH/DELETE /api/labels/:id`                             | Workspace access to read; owner or editor to write; unique name per workspace; version check on rename and delete                                                                         | `LabelReadRepository` + `LabelWriteRepository`; CloudBase adapter reads `labels` through the table route and calls `chronelle_label_create/update/delete` (migration 0036) behind `CLOUDBASE_WRITES_ENABLED`                                                                                                                                                                                                                 |
| Sections: list, create, update, delete | `GET/POST /api/events/:id/sections`, `PATCH/DELETE /api/sections/:id`              | `view` of the Event to read; `edit` of the Event to write; name and description limits; placement among the view's siblings                                                               | `SectionReadRepository` + `SectionWriteRepository`; CloudBase adapter calls `chronelle_section_list` and `chronelle_section_create/update/delete` (migration 0062) behind `CLOUDBASE_WRITES_ENABLED` for writes                                                                                                                                                                                                              |
| Person collection                      | `GET /api/persons`                                                                 | Authorized `view` per candidate, name query, name order, bounded page                                                                                                                     | `PersonReadRepository`; CloudBase adapter reads `objects`, `persons`, `person_contacts`, and `person_labels` through the table route and re-applies visibility in application code                                                                                                                                                                                                                                           |
| Note collection                        | `GET /api/events/:id/notes`                                                        | Authorized `view` of the Event and per note, newest edit or title order, the account behind each current version                                                                          | `NoteReadRepository`; CloudBase adapter calls `chronelle_note_list` (migration 0060), which authorizes and orders in the database                                                                                                                                                                                                                                                                                            |
| Note create and update                 | `POST /api/notes`, `PATCH /api/notes/:id`                                          | Authorization, text limit, CAS on `version`, audit, revision                                                                                                                              | `NoteWriteRepository`; CloudBase adapter calls `chronelle_note_create` and `chronelle_note_update` (migration 0060) behind `CLOUDBASE_WRITES_ENABLED`                                                                                                                                                                                                                                                                        |
| Person create and update               | `POST /api/persons`, `PATCH /api/persons/:id`                                      | Authorization, member link and email rules, CAS on `version`, audit, revision                                                                                                             | `PersonWriteRepository`; CloudBase adapter calls `chronelle_person_create` and `chronelle_person_update` (migration 0037; nickname, description, contacts, and labels since 0050) behind `CLOUDBASE_WRITES_ENABLED`                                                                                                                                                                                                          |
| Task create                            | `POST /api/tasks`                                                                  | Authorization, typed validation, audit event, revision, generated identity                                                                                                                | `TaskWriteRepository`; CloudBase adapter calls `chronelle_task_create` (migrations 0013, 0034 through 0039, 0042, 0044 through 0046, 0061, and 0062) behind `CLOUDBASE_WRITES_ENABLED`                                                                                                                                                                                                                                       |
| Task update                            | `PATCH /api/tasks/:id`                                                             | Authorization, CAS on `version`, merged-state validation, audit, revision                                                                                                                 | `TaskWriteRepository`; CloudBase adapter calls `chronelle_task_update` (migrations 0013, 0034 through 0039, 0044 through 0046, 0061, and 0062) behind `CLOUDBASE_WRITES_ENABLED`                                                                                                                                                                                                                                             |
| Expense create                         | `POST /api/expenses`                                                               | Authorization, typed validation, audit event, revision, generated identity                                                                                                                | `ExpenseWriteRepository`; CloudBase adapter calls `chronelle_expense_create` (migrations 0014 and 0062) behind `CLOUDBASE_WRITES_ENABLED`                                                                                                                                                                                                                                                                                    |
| Expense update                         | `PATCH /api/expenses/:id`                                                          | Authorization, CAS on `version`, merged-state validation, audit, revision                                                                                                                 | `ExpenseWriteRepository`; CloudBase adapter calls `chronelle_expense_update` (migrations 0014 and 0062) behind `CLOUDBASE_WRITES_ENABLED`                                                                                                                                                                                                                                                                                    |
| Reminder create                        | `POST /api/reminders`                                                              | Authorization, typed validation, audit event, revision, generated identity                                                                                                                | `ReminderWriteRepository`; CloudBase adapter calls `chronelle_reminder_create` (migrations 0015 and 0046) behind `CLOUDBASE_WRITES_ENABLED`                                                                                                                                                                                                                                                                                  |
| Reminder update                        | `PATCH /api/reminders/:id`                                                         | Authorization, CAS on `version`, merged-state validation, audit, revision                                                                                                                 | `ReminderWriteRepository`; CloudBase adapter calls `chronelle_reminder_update` (migrations 0015 and 0046) behind `CLOUDBASE_WRITES_ENABLED`                                                                                                                                                                                                                                                                                  |
| Linked creation                        | `POST /api/events/:id/resources`                                                   | Authorization, self-scoped context, typed create, `includes` relation, both audits, idempotent command                                                                                    | `EventContextWriteRepository`; CloudBase adapter calls `chronelle_event_context_create` (migration 0016) behind `CLOUDBASE_WRITES_ENABLED`                                                                                                                                                                                                                                                                                   |
| Soft deletion and restoration          | Object delete, trash recovery, revision restore                                    | CAS, audit, object/revision consistency, recoverability                                                                                                                                   | `ObjectLifecycleWriteRepository`; CloudBase adapter calls `chronelle_object_delete` and `chronelle_object_recover` (migrations 0018 and 0041) and `chronelle_object_restore` (migration 0019) behind `CLOUDBASE_WRITES_ENABLED`                                                                                                                                                                                              |
| Relationship changes                   | Include, attach, paid-for, and removed-link relations                              | Both endpoints remain independent objects; relation audit and authorization                                                                                                               | `RelationWriteRepository`; CloudBase adapter calls `chronelle_relation_create` (0016) and `chronelle_relation_lifecycle` (0017) behind `CLOUDBASE_WRITES_ENABLED`                                                                                                                                                                                                                                                            |
| Sharing and permission-scope changes   | Grant (whole, or narrowed to a view or a section), revoke, leave, stop inheritance | Canonical scope, inherited access narrowed by the grant's view or section (`chronelle_grant_admits`, migration 0063, mirrored in the adapters' re-evaluation), audit, workspace isolation | `ShareWriteRepository` and `PermissionScopeWriteRepository`; CloudBase adapter calls `chronelle_resource_share` (migration 0063: grantee by email, Person, friend, or granted account id; scope and section), `chronelle_resource_share_revoke`, `chronelle_resource_share_leave` (migration 0065: the grantee drops its own grants), and `chronelle_object_scope_update` (migration 0020) behind `CLOUDBASE_WRITES_ENABLED` |
| Event-page layout changes              | Add/remove/reorder panels and restore history                                      | Layout versioning, audit, restoration, optimistic concurrency                                                                                                                             | `EventLayoutWriteRepository`; CloudBase adapter calls `chronelle_event_layout_update` and `chronelle_event_layout_restore` (migration 0022) behind `CLOUDBASE_WRITES_ENABLED`                                                                                                                                                                                                                                                |
| Attachments                            | Upload authorization, finalize, download authorization                             | Private storage, parent authorization, short-lived transfer, audit                                                                                                                        | `DocumentTransferWriteRepository` and `DocumentTransferReadRepository`; CloudBase adapters call `chronelle_document_transfer_authorize`, `chronelle_document_transfer_consume`, and `chronelle_document_finalize` (migration 0026) behind `CLOUDBASE_WRITES_ENABLED`; the storage provider is unchanged                                                                                                                      |
| Undo/redo and commands                 | Execute, undo, redo                                                                | Durable command state, inverse operation, atomic audit and recovery                                                                                                                       | `CommandWriteRepository`; CloudBase adapter calls `chronelle_command_execute` and `chronelle_command_transition` (migration 0023) behind `CLOUDBASE_WRITES_ENABLED`                                                                                                                                                                                                                                                          |

The CloudBase RDB transport advertises no transaction or native TCP
capability for table writes. Mutations move only as PostgreSQL functions
called through the gateway's rpc route, where one call is one transaction, and
only after a differential test proves the function leaves the same resource,
audit, and revision rows as the Drizzle/PostgreSQL service. The Drizzle path
remains a first-class backend that any TCP deployment uses unchanged.

## Gate evidence

Before changing a row in this matrix, add evidence at the same scope as the
operation:

1. Contract tests compare canonical IDs, relation sets, deletion filtering,
   and permission outcomes with the PostgreSQL implementation.
2. Stale-version tests prove a conflict rather than a silent overwrite.
3. Audit assertions prove the event is present for every mutation.
4. Injected-failure tests prove that cross-object operations do not expose
   partial state.
5. Deployment checks prove that credentials remain server-only and private
   document URLs remain authorized and short-lived.

The current evidence satisfies the event-list and calendar read rows in a
local double plus the opt-in real-gateway harness, and the object, relation,
share, revision, and Trash read rows in a local double
(`cloudbase-inventory-read-contract.integration.test.ts`) that runs both
implementations through the same services against one database. The harness
still requires staging workspace, user, and event identifiers before it can
provide real CloudBase evidence, and the inventory adapters have not yet been
exercised against the real gateway. One encoding detail is settled from a
gateway probe: the table select returns `numeric` and `bigint` columns as JSON
numbers, so the adapters request `amount::text` and `size_bytes::text` through
the select cast, which the gateway honours (`"12.5000"` for a numeric amount),
and the strict decoders stay unchanged.
